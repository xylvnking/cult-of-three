/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  swcMinify: true
}

module.exports = nextConfig


module.exports = {
  images: {
    domains: ['cdna.artstation.com', 'cdnb.artstation.com', 'images.unsplash.com'],
  }
}